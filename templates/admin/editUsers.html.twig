{% extends 'admin/admin.html.twig' %}


{% block body %}

    {% block leftsidebar %}

        {{ parent() }}

    {% endblock %}

    {% block rightpart %}

        <div class="right-part edit-users">
            <ul>
                {% for user in users %}

                    <li><span>{{ user.email }}</span>
                        <span>{{ user.firstName }}</span>
                        <span>{{ user.lastName }}</span>
                        <a href="{{ path('delete', {'id': user.id }) }}"> delete </a>
                        <a href="{{ path('role_blogger', {'id': user.id }) }}"> make blogger </a>
                        <a href="{{ path('role_admin', {'id': user.id }) }}"> make admin </a>
                    </li>

                {% endfor %}
            </ul>


        </div>

    {% endblock %}


{% endblock %}